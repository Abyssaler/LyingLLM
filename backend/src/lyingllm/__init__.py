"""LyingLLM backend package.

The :mod:`lyingllm` namespace contains pure domain code, the engine, agents,
LLM adapters, services, and storage. The FastAPI app shell lives in
``backend/app`` and only assembles routers — it doesn't own any rules.
"""

__all__: list[str] = []
